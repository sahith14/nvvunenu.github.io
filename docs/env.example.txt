# =====================================================================
# Nuvvu Nenu — environment template (sample, no live keys)
# Copy this to .env in the project root and fill in real values.
# .env is git-ignored. NEVER commit a file containing live secrets.
# =====================================================================

# --- Firebase (frontend, public-safe) ---
VITE_FIREBASE_API_KEY=YOUR_FIREBASE_API_KEY
VITE_FIREBASE_AUTH_DOMAIN=nuvvunenu-cf326.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=nuvvunenu-cf326
VITE_FIREBASE_STORAGE_BUCKET=nuvvunenu-cf326.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=YOUR_SENDER_ID
VITE_FIREBASE_APP_ID=YOUR_APP_ID
VITE_FIREBASE_MEASUREMENT_ID=YOUR_MEASUREMENT_ID

# --- Supabase (frontend, public-safe — bucket policy enforces access) ---
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# --- Payments (frontend, publishable) ---
VITE_STRIPE_PUBLIC_KEY=pk_live_or_test_key
VITE_RAZORPAY_KEY_ID=

# --- Analytics ---
VITE_GA_MEASUREMENT_ID=

# =====================================================================
# Server-side secrets (set in server/.env or your hosting environment)
# These are NOT prefixed with VITE_ and must NEVER reach the browser.
# =====================================================================
STRIPE_SECRET_KEY=sk_test_or_live_key
RAZORPAY_KEY_SECRET=
OPENAI_API_KEY=
FROM_EMAIL=hello@nuvvunenu.app
